
/*
const calendlyCustomThankYouPageUrl = "https://www.your-brother.com"

export default function isCalendlyEvent(e:MessageEvent): boolean{
    return(e.data.event && e.data.event.indexOf('calendly') == 0);

};
    window.addEventListener('message', function(e: MessageEvent)
    {
     
 }      if(isCalendlyEvent(e: MessageEvent)){
            console.log(e.data);

            if(e.data.event == "calendly.event_type_viewed")
                {
                    console.log("Calendly - viewed");
                }
            
            if(e.data.event == "calendly.date_and_time_selected")
                {
                    console.log("Calendly - Date and time selected");
                }
            
            if(e.data.event == "calendly.event_scheduled")
                {
                    console.log("Calendly-SESSION BOOKED, Lets redirect");
                    window.top.location = calendlyCustomThankYouPageUrl;
                }
            
        }
    })
    console.log("calendly event listener & redirection up and running")


    */